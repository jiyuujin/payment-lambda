{
  "name": "payment-lambda",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "create": "sls create --template aws-nodejs",
    "deploy": "sls deploy",
    "deploy --test": "sls deploy --test",
    "deploy --live": "sls deploy --live",
    "lint": "eslint ./ --ext js",
    "lint:fix": "eslint ./ --ext js --fix"
  },
  "dependencies": {
    "dotenv": "^7.0.0",
    "stripe": "^6.36.0"
  },
  "devDependencies": {
    "@nekohack/eslint-config": "^0.3.0",
    "babel-eslint": "^10.1.0",
    "eslint": "^7.13.0"
  }
}
